"use client"; import {useState} from "react"; import {supabase} from "@/lib/supabase";
export default function Auth(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [mode,setMode]=useState<"signin"|"signup">("signin");const [msg,setMsg]=useState("");async function submit(){setMsg("");if(!supabase){setMsg("Supabase environment variables are not configured yet.");return}const r=mode==="signin"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});setMsg(r.error?.message||"Check your email to continue.")}return <div className="mx-auto max-w-md px-6 py-16"><h1 className="text-4xl font-black">{mode==="signin"?"Welcome back":"Create your account"}</h1><p className="mt-2 text-neutral-500">Your account powers the personal learning center.</p><div className="mt-8 space-y-3"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-2xl border bg-white px-4 py-3"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-2xl border bg-white px-4 py-3"/><button onClick={submit} className="w-full rounded-full bg-black py-3 font-semibold text-white">{mode==="signin"?"Sign in":"Sign up"}</button>{msg&&<p className="rounded-2xl bg-neutral-100 p-4 text-sm">{msg}</p>}</div><button onClick={()=>setMode(mode==="signin"?"signup":"signin")} className="mt-5 text-sm font-semibold underline">{mode==="signin"?"Need an account? Sign up":"Already have an account? Sign in"}</button></div>}
