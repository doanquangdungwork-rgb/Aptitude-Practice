"use client"; import Link from "next/link"; import {useState} from "react"; import {appCatalog,questionsForPillar} from "../../lib/data";
export default function Practice(){const [pillar,setPillar]=useState("");const [sub,setSub]=useState("");const count=pillar?questionsForPillar(pillar,sub||undefined).length:0;return <div className="app-page"><div className="section-head"><div><p className="eyebrow">Section 2 · Practice by type</p><h1 className="section-title">Choose a reasoning pillar.</h1><p className="mt-3 text-sm text-[#99968f]">Focus on one reasoning style when you want a targeted session.</p></div></div><div className="practice-grid">{appCatalog.pillars.map((p:any,i:number)=><button key={p.id} onClick={()=>{setPillar(p.id);setSub("")}} className={`practice-card ${["tone-pink","tone-lavender","tone-lime","tone-mint","tone-sky","tone-peach"][i%6]} text-left`}><span className="practice-index">{String(i+1).padStart(2,"0")}</span><h3>{p.name}</h3><p>{p.description}</p><span className="practice-arrow">{pillar===p.id?"✓":"→"}</span></button>)}</div>{pillar&&<section className="dashboard-section"><p className="eyebrow">Selected pillar</p><h2 className="section-title mt-2">{appCatalog.pillars.find((p:any)=>p.id===pillar)?.name}</h2><div className="mt-5 flex flex-wrap gap-2">{["",...(((appCatalog.subtypes as any)[pillar]||[]))].map((s:string)=><button key={s||"all"} onClick={()=>setSub(s)} className={`quiet-button ${sub===s?"!bg-[#e9f236] !border-[#e9f236]":""}`}>{s?s.replaceAll("_"," "):"All"}</button>)}</div><div className="mt-8 flex items-end justify-between gap-6"><div><div className="text-4xl font-medium">{count.toLocaleString()}</div><div className="mt-1 text-sm text-[#aaa7a0]">questions currently indexed</div></div><Link href={`/practice/${pillar}${sub?`?subtype=${sub}`:""}`} className="yellow-button">Start practice →</Link></div></section>}</div>}
