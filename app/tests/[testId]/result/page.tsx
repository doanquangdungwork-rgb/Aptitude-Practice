"use client";
import Link from "next/link"; import {useParams} from "next/navigation"; import {useEffect,useMemo,useState} from "react"; import {appCatalog,pillarMap,questionsForTest} from "../../../../lib/data"; import {getCompletedAttempt,getWrongQuestions,setWrongQuestions} from "../../../../lib/progress"; import ShareCard from "../../../../components/share-card";
const norm=(v:unknown)=>String(v??"").trim().toLowerCase();const formatTime=(seconds=0)=>`${Math.floor(seconds/60)}m ${seconds%60}s`;
export default function ResultPage(){const {testId}=useParams<{testId:string}>();const test=appCatalog.tests.find((x:any)=>x.test_id===testId);const qs=useMemo(()=>questionsForTest(testId),[testId]);const [attempt,setAttempt]=useState<any>(null);useEffect(()=>setAttempt(getCompletedAttempt(testId)),[testId]);if(!test||!attempt)return <div className="app-page"><div className="quiz-card"><h1 className="section-title">Result unavailable</h1><p className="mt-2 text-sm text-[#99968f]">Complete the test first to see the review.</p><Link href={`/tests/${testId}`} className="yellow-button mt-6 inline-flex">Back to test</Link></div></div>;const answered=qs.filter((q:any)=>attempt.answers?.[q.id]);const correct=qs.filter((q:any)=>norm(attempt.answers?.[q.id])===norm(q.a)).length;const wrong=answered.length-correct;const unanswered=qs.length-answered.length;const accuracy=qs.length?Math.round(correct/qs.length*100):0;const markWrong=(id:string)=>{const ids=getWrongQuestions();setWrongQuestions(ids.includes(id)?ids.filter(x=>x!==id):[...ids,id])};return <div className="app-page"><div className="section-head"><div><p className="eyebrow">Test review</p><h1 className="section-title mt-2">{test.title.replaceAll("_"," ")}</h1><p className="mt-2 text-sm text-[#99968f]">{pillarMap[test.pillar]} · completed {new Date(attempt.completedAt).toLocaleString()}</p></div><div className="flex flex-wrap gap-3"><ShareCard kind="result" result={{title:test.title.replaceAll("_"," "),correct,total:qs.length,accuracy,duration:formatTime(attempt.durationSeconds||0)}}/><Link href="/tests" className="outline-action">All tests</Link></div></div><section className="result-hero"><div><p className="eyebrow">Your result</p><div className="score-number mt-3">{correct}<span className="ml-2 text-3xl text-[#aaa7a0]">/ {qs.length}</span></div><p className="mt-3 text-sm text-[#77736b]">{accuracy}% accuracy · {formatTime(attempt.durationSeconds||0)} spent</p></div><div className="result-metrics"><div className="result-metric"><b>{correct}</b><span>Correct</span></div><div className="result-metric"><b>{wrong}</b><span>Wrong</span></div><div className="result-metric"><b>{unanswered}</b><span>Skipped</span></div></div></section><div className="mt-5 quiz-progress"><span style={{width:`${accuracy}%`}}/></div><div className="section-head mt-12"><div><p className="eyebrow">Question by question</p><h2 className="section-title mt-2">Review your answers.</h2></div><span className="text-xs text-[#aaa7a0]">Scroll to revisit everything</span></div><section className="space-y-4">{qs.map((q:any)=>{const selected=attempt.answers?.[q.id]||"";const isCorrect=norm(selected)===norm(q.a);const skipped=!selected;const opts=(q.o||[]) as any[];return <article key={q.id} className={`review-item ${skipped?"skipped":isCorrect?"correct":"wrong"}`}><div className="flex items-start justify-between gap-4"><span className="eyebrow">Question {q.number}{q.subquestion||""}</span><span className="text-xs font-bold">{skipped?"Skipped":isCorrect?"Correct":"Incorrect"}</span></div><div className="mt-4 text-lg font-medium leading-8 whitespace-pre-wrap">{q.t}</div><div className="mt-5 grid gap-2">{opts.map((o:any,j:number)=>{const val=typeof o==="string"?o:o.text||o.label||o.option||"";const selectedHere=norm(val)===norm(selected);const correctHere=norm(val)===norm(q.a);return <div key={j} className={`review-option ${correctHere?"correct-answer":selectedHere&&!correctHere?"selected-wrong":""}`}><span className="mr-2 font-bold">{String.fromCharCode(65+j)}.</span>{val||`Option ${String.fromCharCode(65+j)}`}{correctHere&&<span className="ml-2 text-xs">✓ Correct answer</span>}{selectedHere&&!correctHere&&<span className="ml-2 text-xs">Your answer</span>}</div>})}</div>{q.explanation&&<div className="mt-5 border-t border-current/10 pt-4 text-sm leading-6 text-[#6f6b64]"><b>Explanation</b><div className="mt-1">{q.explanation}</div></div>}{!skipped&&!isCorrect&&<button onClick={()=>markWrong(q.id)} className="mt-4 outline-action">Add/remove from wrong questions</button>}</article>})}</section></div>}
