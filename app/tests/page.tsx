import Link from "next/link"; import {appCatalog,pillarMap} from "../../lib/data";
export default function Tests(){return <div className="mx-auto max-w-7xl px-6 py-12"><div className="mb-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-neutral-500">Section 1</p><h1 className="mt-2 text-4xl font-black">30 Practice Tests</h1><p className="mt-2 text-neutral-500">29 tests from the gift bank + 1 additional test.</p></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{appCatalog.tests.map((t:any,i:number)=><Link href={`/tests/${t.test_id}`} key={t.test_id} className="rounded-3xl border border-[#e2e0d9] bg-white p-6 hover:shadow-lg"><div className="flex items-start justify-between gap-4"><span className="text-xs font-bold text-neutral-400">TEST {String(i+1).padStart(2,"0")}</span><span className="rounded-full bg-neutral-100 px-3 py-1 text-[11px] font-bold">{pillarMap[t.pillar]}</span></div><h2 className="mt-8 font-bold">{t.title.replaceAll("_"," ")}</h2><p className="mt-2 text-sm text-neutral-500">{t.question_count} questions · timed practice</p><div className="mt-6 h-1 rounded bg-neutral-100"><div className="h-1 w-0 rounded bg-black"/></div></Link>)}</div></div>}
