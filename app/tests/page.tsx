import Link from "next/link"; import {appCatalog,pillarMap} from "../../lib/data";
const tones=["pastel-lavender","pastel-blue","pastel-mint","pastel-peach","pastel-butter","pastel-rose"];
export default function Tests(){return <div className="mx-auto max-w-7xl px-6 py-12 md:py-16"><div className="mb-10 max-w-3xl"><div className="inline-flex rounded-full bg-[#e9e4f7] px-4 py-2 text-xs font-bold uppercase tracking-[.16em] text-[#6f6a8f]">Section 1</div><h1 className="mt-5 text-5xl font-black tracking-[-.045em]">30 Practice Tests</h1><p className="mt-3 text-[#707984]">Full-length aptitude tests from the curated source bank. Pick one and start solving.</p></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{appCatalog.tests.map((t:any,i:number)=><Link href={`/tests/${t.test_id}`} key={t.test_id} className={`soft-card ${tones[i%tones.length]} rounded-[2rem] p-6 transition hover:-translate-y-1 hover:shadow-xl`}><div className="flex items-start justify-between gap-4"><span className="rounded-full bg-white/70 px-3 py-1 text-[11px] font-black tracking-wide">TEST {String(i+1).padStart(2,"0")}</span><span className="rounded-full bg-white/70 px-3 py-1 text-[11px] font-bold">{pillarMap[t.pillar]}</span></div><h2 className="mt-8 text-lg font-black">{t.title.replaceAll("_"," ")}</h2><p className="mt-2 text-sm text-[#6f7782]">{t.question_count} questions · timed practice</p><div className="mt-6 flex items-center justify-between text-sm font-bold"><span className="text-[#737a84]">Ready to start</span><span>→</span></div></Link>)}</div></div>}
